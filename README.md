# media-edit

動画のリストを作成するには

```bash
python3 scripts/generate_videolist.py       
```

手元で`index.html`をビルドするには
```bash
python3 -m http.server 8000
```
を実行して、ブラウザで`http://localhost:8000/index.html`にアクセス。

github pagesにデプロイするには